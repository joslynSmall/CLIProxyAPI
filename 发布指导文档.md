# CLIProxyAPI 发布指导文档

## 目录
- [环境准备](#环境准备)
- [部署流程](#部署流程)
- [验证部署](#验证部署)
- [常见问题](#常见问题)
- [回滚方案](#回滚方案)

---

## 环境准备

### 1. 服务器要求
- **生产主机**: `ssh hk`
- **操作系统**: Linux
- **架构**: `x86_64`
- **部署目录**: `/home/ubuntu/cli-proxy-hk`
- **生产二进制**: `/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin`
- **生产配置**: `/home/ubuntu/cli-proxy-hk/config_hk.yaml`
- **状态文件**: `/home/ubuntu/cli-proxy-hk/state-store.hk.ini`
- **日志文件**: `/home/ubuntu/cli-proxy-hk/logs/app.log`
- **端口**: `18318`

### 2. 配置文件说明

| 文件 | 用途 | `auth-dir` |
|------|------|------------|
| `config.yaml` | 本地开发（不部署到服务器） | `./auths` |
| `config_hk.yaml` | `hk` 生产配置 | `/home/ubuntu/cli-proxy-hk/auths` |

> ⚠️ 当前真实生产环境统一使用 `config_hk.yaml`，不要再按旧的 `227/config-277.yaml` 路径执行。

### 3. 本地准备

#### 3.1 构建生产二进制
```bash
GOOS=linux GOARCH=amd64 go build -o cli-proxy-api.bin.hk ./cmd/server
```

#### 3.2 同步二进制到服务器
```bash
scp ./cli-proxy-api.bin.hk hk:/tmp/cli-proxy-api.bin.new
```

> 只发布二进制时不要覆盖远端 `config_hk.yaml`、`state-store.hk.ini`、`auths/` 和 `logs/`。

#### 3.3 确保关键文件存在
```bash
ssh hk 'ls -la /home/ubuntu/cli-proxy-hk/config_hk.yaml'
ssh hk 'ls -la /home/ubuntu/cli-proxy-hk/state-store.hk.ini'
ssh hk 'ls -la /home/ubuntu/cli-proxy-hk/auths'
```

### 4. 关键配置项说明

**config_hk.yaml 关键字段：**
```yaml
host: "0.0.0.0"
port: 18318
auth-dir: "/home/ubuntu/cli-proxy-hk/auths"
proxy-url: "http://192.168.15.161:7891"

api-key-entries:
  - api-key: your-client-api-key

remote-management:
  allow-remote: true
  secret-key: "$2a$10$..."

routing:
  strategy: "round-robin"
```

**state-store.hk.ini 关键字段：**
```ini
[mongo]
enabled = true
uri = mongodb://cliproxy_app:<PASSWORD>@127.0.0.1:27017/cliproxy_state?authSource=cliproxy_state
database = cliproxy_state
snapshot_collection = service_state_snapshots
flush_interval_seconds = 30
```

---

## 部署流程

### 快速发布（推荐）
```bash
scp ./cli-proxy-api.bin.hk hk:/tmp/cli-proxy-api.bin.new

ssh hk 'set -e
app_dir="/home/ubuntu/cli-proxy-hk"
bin="$app_dir/cli-proxy-api.bin"
cfg="$app_dir/config_hk.yaml"
newbin="/tmp/cli-proxy-api.bin.new"
backup="$app_dir/cli-proxy-api.bin.bak.$(date +%Y%m%d-%H%M%S)-manual-release"

cp "$bin" "$backup"
install -m 0755 "$newbin" "$bin"

old_pids=$(pgrep -f "^$bin -config $cfg$" || true)
if [ -n "$old_pids" ]; then
  kill -15 $old_pids || true
  sleep 3
fi

cd "$app_dir"
nohup "$bin" -config "$cfg" > "$app_dir/logs/app.log" 2>&1 < /dev/null &
'
```

### 详细步骤

#### 1. 记录发布前 PID
```bash
ssh hk 'pgrep -f "^/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml$" | head -n 1'
```

#### 2. 备份当前生产二进制
```bash
ssh hk 'cp /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin.bak.$(date +%Y%m%d-%H%M%S)-manual-release'
```

#### 3. 覆盖二进制
```bash
scp ./cli-proxy-api.bin.hk hk:/tmp/cli-proxy-api.bin.new
ssh hk 'install -m 0755 /tmp/cli-proxy-api.bin.new /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin'
```

#### 4. 重启服务
```bash
ssh hk 'set -e
bin="/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin"
cfg="/home/ubuntu/cli-proxy-hk/config_hk.yaml"
old_pids=$(pgrep -f "^$bin -config $cfg$" || true)
if [ -n "$old_pids" ]; then
  kill -15 $old_pids || true
  sleep 3
fi
cd /home/ubuntu/cli-proxy-hk
nohup "$bin" -config "$cfg" > ./logs/app.log 2>&1 < /dev/null &
'
```

#### 5. 核验 PID 和端口
```bash
old_pid="<发布前PID>"
new_pid=$(ssh hk 'pgrep -f "^/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml$" | head -n 1')
[ -n "$new_pid" ] && [ "$new_pid" != "$old_pid" ] && echo "PID核验通过" || echo "PID核验失败"

ssh hk 'ss -tlnp | grep 18318'
```

> 发布成功标准：必须同时满足 `new_pid` 存在、`new_pid != old_pid`、端口 `18318` 已监听。

---

## 验证部署

### 1. 健康检查
```bash
curl -s http://165.154.3.166:18318/v0/management/usage
curl -H "X-Management-Key: 你的密钥" http://165.154.3.166:18318/v0/management/usage
```

### 2. 查看日志
```bash
ssh hk 'tail -f /home/ubuntu/cli-proxy-hk/logs/app.log'
ssh hk 'tail -30 /home/ubuntu/cli-proxy-hk/logs/app.log'
ssh hk 'head -20 /home/ubuntu/cli-proxy-hk/logs/app.log'
```

### 3. 验证功能
```bash
curl -X POST http://165.154.3.166:18318/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 你的客户端API密钥" \
  -d '{"model": "gpt-4o","messages":[{"role":"user","content":"hello"}]}'
```

### 4. 验证 `glm-5.2` reasoning summary
```bash
curl -X POST http://165.154.3.166:18318/v1/responses \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 你的客户端API密钥" \
  -d '{
    "model": "glm-5.2",
    "stream": false,
    "reasoning": {"effort": "high", "summary": "detailed"},
    "input": [{"role": "user", "content": [{"type": "input_text", "text": "Reply with exactly OK."}]}]
  }'
```

期望返回体里包含：
```json
{
  "output": [
    {
      "type": "reasoning",
      "summary": [
        {"type": "summary_text", "text": "..."}
      ]
    }
  ]
}
```

---

## 常见问题

### Q1: 发布后二进制启动即崩溃
- 先检查上传文件大小和 checksum 是否和本地一致。
- 如果 `scp` 被超时中断，远端可能留下截断 ELF，表现为 `Segmentation fault`。
- 可用以下命令核验：
```bash
ls -lh cli-proxy-api.bin.hk
ssh hk 'ls -lh /tmp/cli-proxy-api.bin.new /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin'
shasum -a 256 cli-proxy-api.bin.hk
ssh hk 'sha256sum /tmp/cli-proxy-api.bin.new /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin'
```

### Q2: 管理 API 返回 `missing management key` 或 `invalid management key`
- 检查 `config_hk.yaml` 中 `remote-management.secret-key`
- 访问 `/v0/management/*` 时需要带 `X-Management-Key`

### Q3: 端口被占用
```bash
ssh hk 'ss -tlnp | grep 18318'
ssh hk 'pgrep -af "^/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml$"'
```

### Q4: 客户端访问代理返回 401
- 检查 `config_hk.yaml` 中顶层 `api-key-entries`
- 调用代理 `/v1/*` 使用的是客户端 key，不是上游 provider 的 `claude-api-key` / `openai-compatibility` key

### Q5: `glm-5.2` 没有返回 reasoning summary
- 检查是否通过真实客户端 key 调用代理，而不是直接调用上游。
- 检查请求是否包含：
```json
"reasoning": {"effort": "high", "summary": "detailed"}
```
- 检查代理日志中 `provider=claude model=glm-5.2` 的 thinking 应用记录。

---

## 回滚方案

### 1. 快速回滚到上一版本
```bash
ssh hk 'set -e
app_dir="/home/ubuntu/cli-proxy-hk"
bin="$app_dir/cli-proxy-api.bin"
cfg="$app_dir/config_hk.yaml"
backup="$app_dir/cli-proxy-api.bin.bak.<备份名>"
cp "$backup" "$bin"
chmod 0755 "$bin"
old_pids=$(pgrep -f "^$bin -config $cfg$" || true)
if [ -n "$old_pids" ]; then kill -15 $old_pids || true; sleep 3; fi
cd "$app_dir"
nohup "$bin" -config "$cfg" > "$app_dir/logs/app.log" 2>&1 < /dev/null &
'
```

### 2. 常用回滚核验
```bash
ssh hk 'pgrep -af "^/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml$"'
ssh hk 'ss -tlnp | grep 18318'
ssh hk 'tail -30 /home/ubuntu/cli-proxy-hk/logs/app.log'
```

---

## 快速参考卡

| 操作 | 命令 |
|------|------|
| 构建生产二进制 | `GOOS=linux GOARCH=amd64 go build -o cli-proxy-api.bin.hk ./cmd/server` |
| 上传二进制 | `scp ./cli-proxy-api.bin.hk hk:/tmp/cli-proxy-api.bin.new` |
| 查看进程 | `ssh hk 'pgrep -af "^/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml$"'` |
| 查看日志 | `ssh hk 'tail -30 /home/ubuntu/cli-proxy-hk/logs/app.log'` |
| 健康检查 | `curl -s http://165.154.3.166:18318/v0/management/usage` |
| 停止服务 | `ssh hk 'pgrep -f "^/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml$" | xargs -r kill -15'` |
| 回滚备份 | `ssh hk 'cp /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin.bak.<备份名> /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin'` |

---

**注意**: 发布前先确认上传的二进制完整无损，发布后必须核验 PID 切换、端口监听和 `glm-5.2` reasoning summary 返回。
