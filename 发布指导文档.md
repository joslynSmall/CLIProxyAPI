# CLIProxyAPI 发布指导文档

## 两环境总览

| 项目 | ssh hk | 227 |
|------|--------|-----|
| SSH 连接 | `ssh hk`（别名，用户 `ubuntu`） | `ssh -i ~/.ssh/id_ed25519 root@192.168.15.227` |
| 远端目录 | `/home/ubuntu/cli-proxy-hk` | `/opt/cliproxy` |
| 二进制文件 | `cli-proxy-api.bin` | `cli-proxy` |
| 配置文件 | `config_hk.yaml` | `config-277.yaml` |
| State Store | `state-store.hk.ini` | `state-store.277.ini` |
| 监听端口 | `18318` | `18318` |
| 远端架构 | Linux x86_64 | Linux ARM64 |
| 构建方式 | **本地构建** → scp 上传 | **远端构建**（rsync 源码后 `/usr/bin/go build`） |
| 启动命令 | `./cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml` | `./cli-proxy -config config-277.yaml` |

### 配置 → State Store 映射规则

| 配置文件名 | 映射的 State Store 文件 |
|---|---|
| `config.yaml` | `state-store.local.ini` |
| `config-277.yaml` | `state-store.277.ini` |
| `config_hk.yaml` | `state-store.hk.ini` |
| `config-<suffix>.yaml` | `state-store.<suffix>.ini` |

> `state-store.*.ini` 不纳入 git，需在各环境单独维护，发布时禁止覆盖远端文件。

---

## 一、发布前准备（通用）

### 1. 运行相关测试

```bash
# 提取本次变更涉及的测试包
git diff main...HEAD --name-only | grep '_test\.go$' | sed 's|/[^/]*$||' | sort -u

# 运行所有变更包的测试
go test -count=1 ./internal/.../ ./sdk/.../ ./cmd/.../ -timeout 120s
```

要求全部通过，0 失败。

### 2. 本地构建（仅 HK 需要）

227 在远端编译，HK 用本地编译产物：

```bash
go build -o cli-proxy-api.bin ./cmd/server
```

---

## 二、ssh hk 发布流程

### 环境信息

- 运行目录：`/home/ubuntu/cli-proxy-hk`
- 运行用户：`ubuntu`
- 本地与远端均为 x86_64，可直接 scp 本地构建产物

### 标准发布步骤

```bash
# 1. 拉回远端生产 YAML（禁止用本地历史版本直接覆盖线上）
cp ./config_hk.yaml ./config_hk.yaml.bak.local-$(date +%Y%m%d-%H%M%S)
scp hk:/home/ubuntu/cli-proxy-hk/config_hk.yaml ./config_hk.yaml

# 2. 在本地基于刚拉回的生产 YAML 做必要修改后，构建
go build -o cli-proxy-api.bin ./cmd/server

# 3. 远端备份
ssh hk "cp /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin.bak.$(date +%Y%m%d-%H%M%S) && cp /home/ubuntu/cli-proxy-hk/config_hk.yaml /home/ubuntu/cli-proxy-hk/config_hk.yaml.bak.$(date +%Y%m%d-%H%M%S)"

# 4. 上传新二进制和生产配置
scp ./cli-proxy-api.bin hk:/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin.new
scp ./config_hk.yaml hk:/home/ubuntu/cli-proxy-hk/config_hk.yaml

# 5. 切换并重启（分步执行，避免 pkill 误杀 SSH 会话）
ssh hk "cd /home/ubuntu/cli-proxy-hk && mv cli-proxy-api.bin.new cli-proxy-api.bin && chmod +x cli-proxy-api.bin"
ssh hk "kill \$(pgrep -x cli-proxy-api.bin | head -1) 2>/dev/null; echo stopped"
sleep 2
ssh hk "cd /home/ubuntu/cli-proxy-hk && nohup ./cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml > logs/app.log 2>&1 < /dev/null & echo started"
```

### 发布后验证

```bash
# 进程与端口
ssh hk 'ps aux | grep cli-proxy-api.bin | grep -v grep; ss -tlnp | grep 18318'

# /v1/models
ssh hk 'curl -sS --max-time 10 -o /dev/null -w "HTTP %{http_code}" -H "Authorization: Bearer <API_KEY>" http://127.0.0.1:18318/v1/models'

# 关键配置项
ssh hk 'grep -nE "request-retry|request-budget-seconds|max-retry-interval|nonstream-keepalive-interval|openai-compat-network-retry|http-429-routing|circuit-breaker-auto-removal" /home/ubuntu/cli-proxy-hk/config_hk.yaml'
```

发布成功标准：
- 新进程存在，命令行包含 `-config /home/ubuntu/cli-proxy-hk/config_hk.yaml`
- `18318` 端口监听正常
- `/v1/models` 返回 `200`
- 新增配置项值与预期一致

### 快速回滚

```bash
# 查看可用备份
ssh hk 'ls -lt /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin.bak.* | head -3'

# 回滚到指定备份
ssh hk 'cd /home/ubuntu/cli-proxy-hk && cp cli-proxy-api.bin.bak.YYYYMMDD_HHMMSS cli-proxy-api.bin && chmod +x cli-proxy-api.bin'
ssh hk "kill \$(pgrep -x cli-proxy-api.bin | head -1) 2>/dev/null"
sleep 2
ssh hk "cd /home/ubuntu/cli-proxy-hk && nohup ./cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml > logs/app.log 2>&1 < /dev/null &"
```

---

## 三、227 发布流程

### 环境信息

- SSH：`root@192.168.15.227`（需 `-i ~/.ssh/id_ed25519`）
- 运行目录：`/opt/cliproxy`
- 远端为 Linux ARM64，**必须在远端编译**
- 远端编译必须使用 `/usr/bin/go`（默认 `go` 可能版本过低）

### 标准发布步骤

```bash
SSH227='ssh -i "$HOME/.ssh/id_ed25519" -o IdentitiesOnly=yes root@192.168.15.227'
DIR=/opt/cliproxy

# 1. 同步源码（排除远端配置、认证和日志）
rsync -avz --delete \
  --exclude '.git' --exclude 'logs' --exclude 'auths' \
  --exclude 'config.yaml' --exclude 'config-277.yaml' \
  --exclude 'config.yaml.*' --exclude 'cli-proxy-new' \
  --exclude 'state-store.*.ini' \
  -e "ssh -i $HOME/.ssh/id_ed25519 -o IdentitiesOnly=yes" \
  ./ "root@192.168.15.227:$DIR/"

# 2. 远端编译
$SSH227 "cd $DIR && /usr/bin/go build -o cli-proxy.new ./cmd/server && chmod +x cli-proxy.new"

# 3. 备份旧二进制并切换
$SSH227 "cd $DIR && ts=\$(date +%Y%m%d_%H%M%S) && cp -f cli-proxy cli-proxy.bak.\$TS 2>/dev/null; mv -f cli-proxy.new cli-proxy && chmod +x cli-proxy"

# 4. 重启
$SSH227 "cd $DIR && pkill -f './cli-proxy -config config-277.yaml' || true; sleep 2 && nohup ./cli-proxy -config config-277.yaml > logs/app.log 2>&1 < /dev/null &"
```

### 发布后验证

```bash
# 进程与端口
$SSH227 'pgrep -af "./cli-proxy -config config-277.yaml"; ss -tlnp | grep 18318'

# /v1/models
curl -sS --max-time 20 -H "Authorization: Bearer <API_KEY>" "http://192.168.15.227:18318/v1/models"

# 流式验证（glm-4.7 Responses）
curl -sS -N --max-time 45 \
  -H "Authorization: Bearer <API_KEY>" \
  -H "Content-Type: application/json" \
  "http://192.168.15.227:18318/v1/responses" \
  -d '{"model":"glm-4.7","input":"只回复: OK","stream":true}'
# 要求流中出现 response.output_text.delta + response.completed
```

### 快速回滚

```bash
$SSH227 'ls -t /opt/cliproxy/cli-proxy.bak.* | head -3'
$SSH227 'cd /opt/cliproxy && cp -f cli-proxy.bak.YYYYMMDD_HHMMSS cli-proxy && chmod +x cli-proxy && pkill -f "./cli-proxy -config config-277.yaml" || true; sleep 2 && nohup ./cli-proxy -config config-277.yaml > logs/app.log 2>&1 < /dev/null &'
```

---

## 四、发布检查清单

每次发布前，对照此表确认新增配置字段已在目标环境的生产 YAML 中配置：

### HK 关键配置项参考值

```yaml
request-retry: 4
request-budget-seconds: 300
max-retry-credentials: 5
max-retry-interval: 30
nonstream-keepalive-interval: 5
openai-compat-network-retry: 2
openai-compat-network-retry-backoff-ms: 500
http-429-routing:
  same-model-failover: true
  routing-policy: immediate_failover
circuit-breaker-auto-removal:
  enabled: true
  auto-remove-threshold: 5
provider-rate-limit:
  enabled: true
  scope: credential
  rate-limit: 40
  rate-window-seconds: 60
  max-stream-concurrency: 5
default-reasoning-on-ingress-by-format:
  claude: { policy: missing_only, mode: adaptive_effort, value: high }
  gemini: { policy: missing_only, mode: level, value: medium }
  openai: { policy: missing_only, mode: effort, value: high }
```

### 227 关键配置项参考值

```yaml
request-retry: 4
request-budget-seconds: 300
max-retry-interval: 30
nonstream-keepalive-interval: 5
```

> 具体值以各环境实际需求为准。新版本如果新增了配置字段（参考 `config.example.yaml`），发布前需确认目标环境的生产 YAML 已包含该字段。

---

## 五、常见问题

### Q1: pkill 导致 SSH 连接中断

**现象**：`ssh hk "pkill -f cli-proxy-api.bin ..."` 返回 exit 255。
**原因**：pkill 的 `-f` 模式可能匹配到 SSH 会话的 bash 进程。
**解决**：分步执行——先 `kill $(pgrep -x cli-proxy-api.bin)`，再 `nohup` 启动。

### Q2: 227 远端 go 版本过低

**现象**：`go.mod requires go >= 1.26.0`
**解决**：显式使用 `/usr/bin/go` 而非默认 `go`。

### Q3: 227 上传了错误平台的二进制

**原因**：本地 macOS 编译的产物不能在 Linux ARM64 上运行。
**解决**：227 必须在远端编译。HK 因为架构相同（x86_64），可以直接 scp 本地构建产物。

### Q4: 覆盖了远端生产配置或 State Store

**风险**：API Key 丢失、上游渠道配置被覆盖、MongoDB 凭据失效。
**预防**：
- `rsync` 时排除 `config-277.yaml`、`config_hk.yaml`、`auths/`、`logs/`、`state-store.*.ini`
- HK 发布前先 `scp` 拉回远端配置，在拉回的基础上修改

### Q5: state-store.*.ini 中 MongoDB 凭据错误

**现象**：启动日志 `mongostate: ping: ... Authentication failed`，进程退出。
**原因**：State Store 中的 MongoDB 用户名/密码/数据库不匹配。
**解决**：检查远端 `state-store.<env>.ini` 中 `[mongo]` 段的 `uri` 和 `database`。

### Q6: 频繁出现 `context canceled`

- 查看 `Error Category: context_canceled` + `Cancel Source: downstream_cancelled` → 下游先断开
- 若在 ~60s 处发生，检查客户端/网关的 idle timeout
- 确认 `nonstream-keepalive-interval: 5` 已设置

### Q7: 频繁出现 `upstream_timeout: upstream request budget exceeded`

- `request-budget-seconds` 总预算耗尽
- 检查 `request-retry` 和 `max-retry-interval` 是否过大
- HK 推荐值：`request-retry: 4`、`max-retry-interval: 30`、`request-budget-seconds: 300`

---

## 六、日志排查命令

### HK

```bash
# 实时日志
ssh hk 'tail -f /home/ubuntu/cli-proxy-hk/logs/app.log'

# 请求日志中的错误分类
ssh hk 'grep -E "Error Category|Cancel Source" /home/ubuntu/cli-proxy-hk/logs/request-$(date +%F).log | tail -60'
```

### 227

```bash
# 实时日志
ssh -i ~/.ssh/id_ed25519 root@192.168.15.227 'tail -f /opt/cliproxy/logs/app.log'

# 最近 30 行
ssh -i ~/.ssh/id_ed25519 root@192.168.15.227 'tail -30 /opt/cliproxy/logs/app.log'
```
