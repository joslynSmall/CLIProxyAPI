# CLIProxyAPI 发布指导文档

## 目录
- [环境准备](#环境准备)
- [部署流程](#部署流程)
- [验证部署](#验证部署)
- [常见问题](#常见问题)
- [回滚方案](#回滚方案)

---

## 环境准备

### 1. 服务器要求
- **操作系统**: Linux (CentOS/Ubuntu/Debian)
- **架构**: x86_64 / ARM64
- **依赖**: Go 1.21+ (编译时), 无需运行时依赖 (静态编译)
- **端口**: 18318 (默认, 可配置)

### 2. 配置文件说明

| 文件 | 用途 | `auth-dir` |
|------|------|------------|
| `config.yaml` | 本地开发（不部署到服务器） | `./auths` |
| `config-277.yaml` | 227 服务器生产配置 | `/opt/cliproxy/auths` |
| `config_hk.yaml` | ssh hk 服务器生产配置 | `/home/ubuntu/cli-proxy-hk/auths` |

> ⚠️ 生产环境统一使用 `config-277.yaml`，`rsync` 时会排除本地 `config.yaml`。

> ssh hk 环境使用 `/home/ubuntu/cli-proxy-hk/config_hk.yaml`，发布时必须同步该生产 YAML，不能用本地 `config.yaml` 覆盖。

### 3. 本地准备

#### 2.1 同步二进制文件到服务器
```bash
# 方式一: 使用 scp
scp -i "$HOME/.ssh/id_ed25519" ./cli-proxy root@192.168.15.227:/opt/cliproxy/

# 方式二: 同步整个目录
rsync -avz --exclude '.git' --exclude 'worktrees' --exclude 'config.yaml' --exclude 'config-277.yaml' --exclude 'config.yaml.*' --exclude 'state-store.ini' --exclude 'logs' --exclude 'auths' \
  -e "ssh -i $HOME/.ssh/id_ed25519" \
  ./ root@192.168.15.227:/opt/cliproxy/
```

> `state-store.local.ini` 和 `state-store.277.ini` 必须在各自环境分别维护，发布同步时禁止覆盖远端文件。

#### 2.2 确保配置文件存在
```bash
# 检查远程服务器配置文件
ssh -i "$HOME/.ssh/id_ed25519" root@192.168.15.227 "ls -la /opt/cliproxy/config-277.yaml"

# 检查运行时状态持久化配置文件
ssh -i "$HOME/.ssh/id_ed25519" root@192.168.15.227 "ls -la /opt/cliproxy/state-store.277.ini"
```

**config-277.yaml 关键配置项说明:**
```yaml
host: "0.0.0.0"
port: 18318
auth-dir: "/opt/cliproxy/auths"      # 认证文件目录
proxy-url: "http://192.168.15.161:7891"  # 上游代理
api-keys:
  - your-api-key-here

# 远程管理配置
remote-management:
  allow-remote: true
  secret-key: "$2a$10$..."          # bcrypt hash 管理密钥

# 路由策略
routing:
  strategy: "round-robin"           # 轮询/权重

# 超时与保活
request-retry: 4                     # auth/credential 级兜底重试，不宜过大
request-budget-seconds: 300          # 单请求总预算，包含重试与冷却等待
max-retry-interval: 30               # 凭据冷却重试前最多等待秒数
nonstream-keepalive-interval: 5      # 非流式等待上游时每 5 秒写空行保活
```

**state-store.277.ini 关键配置项说明:**
```ini
[mongo]
enabled = true
uri = mongodb://cliproxy_app:<PASSWORD>@127.0.0.1:27017/cliproxy_state?authSource=cliproxy_state
database = cliproxy_state
snapshot_collection = service_state_snapshots
flush_interval_seconds = 30
```

- 本地开发时，`config.yaml` 优先读取 `state-store.local.ini`
- 生产服务器上，`config-277.yaml` 优先读取 `state-store.277.ini`
- 这些文件都不纳入 git，需在目标环境单独维护

---

## 部署流程

### ssh hk 发布流程

ssh hk 当前运行目录为 `/home/ubuntu/cli-proxy-hk`，运行命令为：

```bash
/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml
```

发布步骤：

```bash
# 1. 本地构建，Go 版本需与 go.mod 对齐
go build -o cli-proxy-api.bin ./cmd/server

# 2. 远端备份二进制与生产配置
ssh hk "cp /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin.bak.$(date +%Y%m%d-%H%M%S) && cp /home/ubuntu/cli-proxy-hk/config_hk.yaml /home/ubuntu/cli-proxy-hk/config_hk.yaml.bak.$(date +%Y%m%d-%H%M%S)"

# 3. 同步新二进制与 hk 生产 YAML
scp ./cli-proxy-api.bin hk:/home/ubuntu/cli-proxy-hk/cli-proxy-api.bin.new
scp ./config_hk.yaml hk:/home/ubuntu/cli-proxy-hk/config_hk.yaml

# 4. 切换并重启
ssh hk "cd /home/ubuntu/cli-proxy-hk; mv cli-proxy-api.bin.new cli-proxy-api.bin; chmod +x cli-proxy-api.bin; pkill -f 'cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml' || true; nohup /home/ubuntu/cli-proxy-hk/cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml > /home/ubuntu/cli-proxy-hk/logs/app.log 2>&1 &"

# 5. 验证进程、端口和生产配置
ssh hk "pgrep -af 'cli-proxy-api.bin -config /home/ubuntu/cli-proxy-hk/config_hk.yaml'; ss -tlnp | grep 18318; grep -nE 'request-retry|request-budget-seconds|max-retry-interval|nonstream-keepalive-interval' /home/ubuntu/cli-proxy-hk/config_hk.yaml"
```

发布成功标准：
- 新进程存在，且命令行包含 `-config /home/ubuntu/cli-proxy-hk/config_hk.yaml`。
- `18318` 端口监听正常。
- 生产 YAML 中 `request-budget-seconds`、`request-retry`、`max-retry-interval`、`nonstream-keepalive-interval` 与预期一致。
- `/v1/models` 使用生产 API key 返回 `200`。

### 快速启动 (推荐)

```bash
# 一条命令完成重启
ssh -i "$HOME/.ssh/id_ed25519" -o IdentitiesOnly=yes -o StrictHostKeyChecking=no root@192.168.15.227 \
  "pkill -f cli-proxy 2>/dev/null; cd /opt/cliproxy && nohup ./cli-proxy -config config-277.yaml > /tmp/cliproxy.log 2>&1 &"
```

### 详细步骤

#### 1. 停止旧进程
```bash
# 先记录发布前 PID（用于发布后核验）
ssh root@192.168.15.227 "pgrep -f './cli-proxy -config config-277.yaml' | head -n 1"

# 停止旧进程
ssh root@192.168.15.227 "pkill -f cli-proxy 2>/dev/null || true"
```

#### 2. 启动服务
```bash
# 后台启动
ssh root@192.168.15.227 "cd /opt/cliproxy && nohup ./cli-proxy -config config-277.yaml > /tmp/cliproxy.log 2>&1 &"

# 或前台启动查看输出 (调试用)
ssh root@192.168.15.227 "cd /opt/cliproxy && ./cli-proxy -config config-277.yaml"
```

#### 3. 验证进程
```bash
# 检查进程是否存在
ssh root@192.168.15.227 "ps aux | grep cli-proxy | grep -v grep"

# 核验发布前后 PID 是否已切换（示例）
# old_pid 为停止前记录的 PID
# new_pid 为重启后最新 PID
old_pid="<停止前PID>"
new_pid=$(ssh root@192.168.15.227 "pgrep -f './cli-proxy -config config-277.yaml' | head -n 1")
[ -n "$new_pid" ] && [ "$new_pid" != "$old_pid" ] && echo "PID核验通过" || echo "PID核验失败"

# 检查端口是否监听
ssh root@192.168.15.227 "ss -tlnp | grep 18318"
```

> 发布核验要求：必须同时满足 `new_pid` 存在且 `new_pid != old_pid`，否则不能判定发布成功。

---

## 验证部署

### 1. 健康检查
```bash
# 测试 API 响应 (需要管理密钥)
curl http://192.168.15.227:18318/v0/management/usage

# 带管理密钥
curl -H "X-Management-Key: 你的密钥" http://192.168.15.227:18318/v0/management/usage
```

### 2. 查看日志
```bash
# 实时日志
ssh root@192.168.15.227 "tail -f /tmp/cliproxy.log"

# 最后 30 行
ssh root@192.168.15.227 "tail -30 /tmp/cliproxy.log"

# 启动日志 (查看服务初始化)
ssh root@192.168.15.227 "head -20 /tmp/cliproxy.log"
```

### 3. 验证功能
```bash
# 测试代理请求是否正常
curl -X POST http://192.168.15.227:18318/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 你的API密钥" \
  -d '{"model": "gpt-4o","messages":[{"role":"user","content":"hello"}]}'
```

### 4. 验证超时分类日志

`request-log: true` 时，请求日志写入 `logs/request-YYYY-MM-DD.log`。本次超时治理后，重点看以下字段：

```text
Error Category: context_canceled
Cancel Source: downstream_cancelled
```

表示下游客户端、网关或反向代理先断开，导致上游请求被取消。

```text
Error Category: request_budget_exceeded
Cancel Source: request_budget
```

表示服务端 `request-budget-seconds` 总预算耗尽，属于代理主动终止。

线上排查命令示例：

```bash
ssh hk "grep -n 'Error Category\|Cancel Source\|context canceled\|upstream_timeout: upstream request budget exceeded' /home/ubuntu/cli-proxy-hk/logs/request-$(date +%F).log | tail -n 60"
```

---

## 常见问题

### Q1: 启动后无日志输出
```bash
# 检查日志文件是否创建
ssh root@192.168.15.227 "ls -la /tmp/cliproxy.log"

# 如果文件不存在，手动创建目录并重新启动
ssh root@192.168.15.227 "cd /opt/cliproxy && ./cli-proxy -config config-277.yaml 2>&1 | tee /tmp/cliproxy.log"
```

### Q2: 端口被占用
```bash
# 查找占用进程
ssh root@192.168.15.227 "ss -tlnp | grep 18318"

# 强制终止
ssh root@192.168.15.227 "kill -9 <PID>"
```

### Q3: 管理 API 返回 "invalid management key"
- 检查 config-277.yaml 中 `remote-management.secret-key` 是否正确
- 使用 bcrypt 加密的密钥，或在配置中设置明文密钥 (启动时会自动 hash)

### Q4: 连接上游代理失败
```bash
# 检查上游代理连通性
ssh root@192.168.15.227 "curl -x http://192.168.15.161:7891 https://api.openai.com/v1/models"

# 检查 config-277.yaml 中 proxy-url 配置
```

### Q5: 认证文件加载失败
```bash
# 检查 auths 目录权限和文件
ssh root@192.168.15.227 "ls -la /opt/cliproxy/auths/"

# 检查认证文件格式
ssh root@192.168.15.227 "cat /opt/cliproxy/auths/*.json | head -50"
```

### Q6: 频繁出现 `context canceled`

- 优先判断是否是下游主动断开：查看请求日志中是否存在 `Error Category: context_canceled` 与 `Cancel Source: downstream_cancelled`。
- 如果发生在约 60 秒附近，重点检查客户端、Nginx/Caddy/负载均衡或平台网关的 idle/read timeout。
- 对非流式长请求，确认生产 YAML 中已设置 `nonstream-keepalive-interval: 5`，且运行进程确实加载生产 YAML。

### Q7: 频繁出现 `upstream_timeout: upstream request budget exceeded`

- 该错误表示 `request-budget-seconds` 总预算耗尽，包含请求执行、重试和冷却等待。
- 先检查生产 YAML 是否有过大的 `request-retry` 或 `max-retry-interval`。ssh hk 推荐值为 `request-retry: 4`、`max-retry-interval: 30`、`request-budget-seconds: 300`。
- 如果仍频繁出现，说明上游本身长时间无响应或请求体过大，应优先切换更稳定的上游节点或降低高推理模型请求量，而不是继续增加重试次数。

---

## 回滚方案

### 1. 快速回滚到上一版本
```bash
# 停止当前版本
ssh root@192.168.15.227 "pkill -f cli-proxy"

# 恢复备份
ssh root@192.168.15.227 "cd /opt/cliproxy && cp cli-proxy.bak.* cli-proxy"

# 重启
ssh root@192.168.15.227 "cd /opt/cliproxy && nohup ./cli-proxy -config config-277.yaml > /tmp/cliproxy.log 2>&1 &"
```

### 2. 使用 start.sh 管理 (推荐)
```bash
# 在服务器上执行
ssh root@192.168.15.227 "cd /opt/cliproxy && ./start.sh stop"
ssh root@192.168.15.227 "cd /opt/cliproxy && ./start.sh start"
```

### 3. 版本备份脚本
```bash
#!/bin/bash
# 部署前备份
DATE=$(date +%Y%m%d_%H%M%S)
ssh root@192.168.15.227 "cp /opt/cliproxy/cli-proxy /opt/cliproxy/cli-proxy.bak.\$DATE"
```

---

## 快速参考卡

| 操作 | 命令 |
|------|------|
| 重启服务 | `ssh root@192.168.15.227 "pkill -f cli-proxy; cd /opt/cliproxy && nohup ./cli-proxy -config config-277.yaml > /tmp/cliproxy.log 2>&1 &"` |
| 查看进程 | `ssh root@192.168.15.227 "ps aux \| grep cli-proxy \| grep -v grep"` |
| 查看日志 | `ssh root@192.168.15.227 "tail -30 /tmp/cliproxy.log"` |
| 健康检查 | `curl -s http://192.168.15.227:18318/v0/management/usage` |
| 停止服务 | `ssh root@192.168.15.227 "pkill -f cli-proxy"` |

---

## 配置模板

### config-277.yaml 最小配置（生产）
```yaml
host: "0.0.0.0"
port: 18318
auth-dir: "/opt/cliproxy/auths"
proxy-url: "http://192.168.15.161:7891"
api-keys:
  - your-api-key

# 允许远程管理
remote-management:
  allow-remote: true
  secret-key: "your-management-key"

# 路由策略
routing:
  strategy: "round-robin"
```

---

**注意**: 部署前确保服务器防火墙已开放 18318 端口。
